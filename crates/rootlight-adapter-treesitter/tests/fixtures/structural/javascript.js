/** Root-level documentation Ω. */

import value from "./dep.js";

class Greeter {
  /** Method documentation. */
  greet(name) {
    const text = "Hello 🌍";
    console.log(name);
    return text;
  }
}
