# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
name = "tree-sitter-astro-next"
version = "0.1.1"
build = "build.rs"
include = [
    "src/**/*",
    "queries/*",
    "build.rs",
    "LICENSE*",
    "README.md",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Astro grammar for tree-sitter, compatible with tree-sitter 0.25+"
homepage = "https://github.com/PRRPCHT/tree-sitter-astro-next"
documentation = "https://docs.rs/tree-sitter-astro-next"
readme = "README.md"
keywords = [
    "tree-sitter",
    "astro",
    "parsing",
    "syntax",
    "grammar",
]
categories = [
    "parsing",
    "text-editors",
]
license = "Apache-2.0 AND MIT"
repository = "https://github.com/PRRPCHT/tree-sitter-astro-next"

[package.metadata.docs.rs]
all-features = true
targets = ["x86_64-unknown-linux-gnu"]

[lib]
name = "tree_sitter_astro_next"
path = "src/lib.rs"

[dependencies.tree-sitter-language]
version = "0.1.7"

[dev-dependencies.tree-sitter]
version = "0.26.5"

[build-dependencies.cc]
version = "1.2.55"

# Local qualification keeps upstream tests outside the production workspace.
[workspace]
