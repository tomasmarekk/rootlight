{
  "$id": "https://rootlight.local/schema/mcp/code.dead/input/1.1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "CodeDeadInput",
  "description": "Strict input for `code.dead`.",
  "type": "object",
  "properties": {
    "budget": {
      "description": "Optional response limits.",
      "anyOf": [
        {
          "$ref": "#/$defs/ResponseBudget"
        }
      ]
    },
    "entry_point_policy": {
      "description": "Entry-point model policy or caller-supplied stable entry symbols.",
      "anyOf": [
        {
          "$ref": "#/$defs/EntryPointPolicy"
        }
      ]
    },
    "explain": {
      "description": "Return the bounded plan without executing retrieval.",
      "type": "boolean"
    },
    "generation": {
      "description": "Immutable generation selector.",
      "anyOf": [
        {
          "$ref": "#/$defs/GenerationSelector"
        }
      ]
    },
    "include_exported": {
      "description": "Whether to include exported symbols as candidates.",
      "type": "boolean"
    },
    "include_tests": {
      "description": "Whether to include test symbols as candidates.",
      "type": "boolean"
    },
    "max_candidates": {
      "description": "Maximum returned dead-code candidates.",
      "type": "integer",
      "format": "uint16",
      "maximum": 500,
      "minimum": 1
    },
    "min_confidence": {
      "description": "Minimum reachability edge confidence.",
      "type": "integer",
      "format": "uint16",
      "maximum": 1000,
      "minimum": 0
    },
    "repository": {
      "description": "Owning repository.",
      "$ref": "#/$defs/RepositorySelector"
    },
    "response_profile": {
      "description": "Requested representation.",
      "anyOf": [
        {
          "$ref": "#/$defs/ResponseProfile"
        }
      ]
    },
    "scope": {
      "description": "Optional structural scope constraint.",
      "anyOf": [
        {
          "$ref": "#/$defs/ScopeSelector"
        }
      ]
    }
  },
  "additionalProperties": false,
  "required": [
    "repository"
  ],
  "$defs": {
    "ActiveGeneration": {
      "description": "Active-generation keyword.",
      "oneOf": [
        {
          "description": "Select the active generation.",
          "type": "string",
          "const": "active"
        }
      ]
    },
    "BuildTargetScope": {
      "description": "Build-target scope.",
      "type": "object",
      "properties": {
        "build_targets": {
          "description": "Distinct build-target identities.",
          "type": "array",
          "items": {
            "type": "string",
            "maxLength": 512,
            "minLength": 1
          },
          "maxItems": 256,
          "minItems": 1,
          "uniqueItems": true
        }
      },
      "additionalProperties": false,
      "required": [
        "build_targets"
      ]
    },
    "EntryPointPolicy": {
      "description": "Entry-point policy accepted by `code.dead`.",
      "anyOf": [
        {
          "description": "One predefined bounded entry model.",
          "$ref": "#/$defs/NamedEntryPointPolicy"
        },
        {
          "description": "A caller-supplied stable root set.",
          "$ref": "#/$defs/ExplicitEntryPointPolicy"
        }
      ]
    },
    "ExplicitEntryPointPolicy": {
      "description": "Caller-supplied complete static entry roots.",
      "type": "object",
      "properties": {
        "entry_symbols": {
          "description": "Distinct stable entry symbols.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/SymbolId"
          },
          "maxItems": 64,
          "minItems": 1,
          "uniqueItems": true
        }
      },
      "additionalProperties": false,
      "required": [
        "entry_symbols"
      ]
    },
    "GenerationId": {
      "type": "string",
      "pattern": "^gen1_[a-z2-7]{39}$"
    },
    "GenerationSelector": {
      "description": "Generation selector shared by first-slice read tools.",
      "anyOf": [
        {
          "description": "Resolve the currently active immutable generation.",
          "$ref": "#/$defs/ActiveGeneration"
        },
        {
          "description": "Pin an explicit immutable generation.",
          "$ref": "#/$defs/GenerationId"
        }
      ]
    },
    "NamedEntryPointPolicy": {
      "description": "Named entry-point model policy for bounded static reachability observations.",
      "oneOf": [
        {
          "description": "Partial mixed entry-point model.",
          "type": "string",
          "const": "standard"
        },
        {
          "description": "Library export surface.",
          "type": "string",
          "const": "library"
        },
        {
          "description": "Application mains and registered handlers.",
          "type": "string",
          "const": "application"
        },
        {
          "description": "Framework routes, services, and registrations.",
          "type": "string",
          "const": "framework_specific"
        }
      ]
    },
    "PackageScope": {
      "description": "Package scope.",
      "type": "object",
      "properties": {
        "packages": {
          "description": "Distinct package identities.",
          "type": "array",
          "items": {
            "type": "string",
            "maxLength": 512,
            "minLength": 1
          },
          "maxItems": 256,
          "minItems": 1,
          "uniqueItems": true
        }
      },
      "additionalProperties": false,
      "required": [
        "packages"
      ]
    },
    "PathScope": {
      "description": "Repository-relative path scope.",
      "type": "object",
      "properties": {
        "paths": {
          "description": "Distinct repository-relative paths.",
          "type": "array",
          "items": {
            "type": "string",
            "maxLength": 8192,
            "minLength": 1
          },
          "maxItems": 256,
          "minItems": 1,
          "uniqueItems": true
        }
      },
      "additionalProperties": false,
      "required": [
        "paths"
      ]
    },
    "ProvenanceLevel": {
      "description": "Provenance detail requested by a read tool.",
      "oneOf": [
        {
          "description": "Omit provenance detail beyond mandatory source identity.",
          "type": "string",
          "const": "none"
        },
        {
          "description": "Return compact provider and confidence evidence.",
          "type": "string",
          "const": "compact"
        },
        {
          "description": "Return maximum bounded provenance evidence.",
          "type": "string",
          "const": "full"
        }
      ]
    },
    "RepositoryId": {
      "type": "string",
      "pattern": "^repo1_[a-z2-7]{32}$"
    },
    "RepositoryIdSelector": {
      "description": "Stable repository-ID selector.",
      "type": "object",
      "properties": {
        "repository_id": {
          "description": "Repository identity.",
          "$ref": "#/$defs/RepositoryId"
        }
      },
      "additionalProperties": false,
      "required": [
        "repository_id"
      ]
    },
    "RepositorySelector": {
      "description": "Repository selector accepted by first-slice read tools.",
      "anyOf": [
        {
          "description": "Select by stable repository identifier.",
          "$ref": "#/$defs/RepositoryIdSelector"
        }
      ]
    },
    "ResponseBudget": {
      "description": "Optional response limits that can only reduce server hard limits.",
      "type": "object",
      "properties": {
        "evidence_level": {
          "description": "Requested evidence detail.",
          "anyOf": [
            {
              "$ref": "#/$defs/ProvenanceLevel"
            }
          ]
        },
        "max_depth": {
          "description": "Maximum plan depth.",
          "type": "integer",
          "format": "uint8",
          "maximum": 16,
          "minimum": 1
        },
        "max_paths": {
          "description": "Maximum independently returned paths.",
          "type": "integer",
          "format": "uint16",
          "maximum": 1000,
          "minimum": 1
        },
        "max_results": {
          "description": "Maximum returned result objects.",
          "type": "integer",
          "format": "uint16",
          "maximum": 1000,
          "minimum": 1
        },
        "max_source_bytes": {
          "description": "Maximum source bytes before JSON escaping.",
          "type": "integer",
          "format": "uint32",
          "maximum": 524288,
          "minimum": 1
        },
        "max_tokens": {
          "description": "Maximum estimated output tokens.\n\nThe schema ceiling is the largest aggregate budget any tool accepts\n(`query.batch` allows up to 16000); the server clamps each request to\nthe calling tool's own hard limit.",
          "type": "integer",
          "format": "uint16",
          "maximum": 16000,
          "minimum": 100
        },
        "max_traversal_facts": {
          "description": "Maximum relationship or traversal facts examined.",
          "type": "integer",
          "format": "uint32",
          "maximum": 100000,
          "minimum": 1
        },
        "timeout_ms": {
          "description": "Cooperative request deadline in milliseconds.",
          "type": "integer",
          "format": "uint32",
          "maximum": 30000,
          "minimum": 10
        }
      },
      "additionalProperties": false
    },
    "ResponseProfile": {
      "description": "Requested response representation.",
      "oneOf": [
        {
          "description": "Smallest complete correctness-bearing response.",
          "type": "string",
          "const": "compact"
        },
        {
          "description": "More explanatory response within the same hard budgets.",
          "type": "string",
          "const": "standard"
        },
        {
          "description": "Maximum bounded provenance and evidence.",
          "type": "string",
          "const": "evidence"
        }
      ]
    },
    "ScopeSelector": {
      "description": "Common scope selector for read tools.",
      "anyOf": [
        {
          "description": "Limit to repository-relative paths.",
          "$ref": "#/$defs/PathScope"
        },
        {
          "description": "Limit to packages.",
          "$ref": "#/$defs/PackageScope"
        },
        {
          "description": "Limit to build targets.",
          "$ref": "#/$defs/BuildTargetScope"
        },
        {
          "description": "Limit to stable symbols.",
          "$ref": "#/$defs/SymbolScope"
        }
      ]
    },
    "SymbolId": {
      "type": "string",
      "pattern": "^sym1_[a-z2-7]{39}$"
    },
    "SymbolScope": {
      "description": "Stable-symbol scope.",
      "type": "object",
      "properties": {
        "symbols": {
          "description": "Distinct symbol identities.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/SymbolId"
          },
          "maxItems": 64,
          "minItems": 1,
          "uniqueItems": true
        }
      },
      "additionalProperties": false,
      "required": [
        "symbols"
      ]
    }
  }
}
