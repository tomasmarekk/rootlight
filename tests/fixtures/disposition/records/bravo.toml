schema_version = "1.0"
id = "bravo"
title = "Blocking gate stage"
implementation_status = "present"
acceptance = "fallback"
gate_outcome = "fallback"
source_revision = "a9487e06a9d8820500383e55bff93f72a68003ec"
milestone = "milestones/bravo.md"
completion_report = "tests/fixtures/disposition/reports/bravo.md"
evidence = ["tests/fixtures/disposition/evidence/bravo.json"]
fallback_boundary = "Bounded synthetic fallback used only to exercise the validator."
dependencies = ["alpha"]
dependents = ["charlie"]
residual_risks = []
